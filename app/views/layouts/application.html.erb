<!DOCTYPE html>
<html>
  <head>
    <title>DiagonAlley</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="shortcut icon" href="<%= image_url("title-image.png") %>" type="image/x-icon" />

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag "application", media: "all", 'data-turbolinks-track': "reload" %>
    <%= javascript_pack_tag "application", 'data-turbolinks-track': "reload" %>
  </head>

  <body class="<%= @bg %>">

  <% if @current_user %>
    <div class="title-container">
      <label class="title">
        <h2>Diagon Alley</h2>
        <h6>Online site for all your magical supplies!</h6>
      </label>

      <% if flash[:error] %>
        <p class="user-error Flash-error">
          <%= flash[:error] %>
        </p>
      <% end %>

      <% if flash[:notice] %>
        <p class="user-error Flash-notice">
          <%= flash[:notice] %>
        </p>
      <% end %>

      <% if flash[:welcome] %>
        <p class="Flash-welcome">
          <%= flash[:welcome] %>
        </p>
      <% end %>


      <%= button_to "Sign-out", destroy_session_path, method: "delete", class: "sign-out-button button" %>
    </div>
  <% end %>

  <%= yield %>


    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script language="JavaScript" type="text/javascript">
      $(function() {
        $('nav.shop-title-bar').click( function(e) {
          $('.collapse').collapse('hide');
        });
      });
    </script>
  </body>
</html>
